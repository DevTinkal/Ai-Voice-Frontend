# Leave empty (or localhost) — when you open the dashboard via a LAN IP
# (e.g. http://192.168.5.49:5173), the app auto-uses the Vite proxy so
# Backend/WS work the same as on localhost.
VITE_API_BASE_URL=
VITE_DASHBOARD_WS_URL=
VITE_TWILIO_PHONE_NUMBER=+16413374825
